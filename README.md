Image Blurring
- This C++ code takes an image and allows the user to blur any regions by dragging the cursor over the desired region.
- After selecting a region, press 'I' to increase the blur or 'D' to decrease it.
- Press 'S' for a copy to be saved of the modified image.
- Press 'R' to reset the image.
- Press escape to close the program.
